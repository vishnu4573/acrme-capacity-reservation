"""ACRME POC Test Suite.

A production-quality Python test-automation package for validating the Azure
Capacity Reservation Management Engine (ACRME) proof-of-concept programme.

The suite drives the Azure CLI (`az`) and `az rest` via subprocess only — it
deliberately depends on NO Azure SDK Python packages.
"""

__version__ = "1.0.0"
